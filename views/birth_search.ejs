<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Search Results</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {  color: black; font-family:system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif; }
  </style>
</head>
<body class="container mt-4">
          <ul class="nav justify-content-start mb-2">
    <li class="nav-item">
      <a class="nav-link text-danger" href="births" style="font-size: x-large;"> ← return </a>
    </li>
  </ul>


  <h1 class="text-danger">Search Results for "<%= query %>"</h1>
  <ul class="list-group">
    <% if (birthResults && birthResults.length > 0) { %>
      <% birthResults.forEach(item => { %>
        <li class="list-group-item bg-dark text-white border-0">
          <%= item.year %> — <%= item.text %>
        </li>
      <% }) %>
    <% } else { %>
      <p>No matching births found.</p>
    <% } %>
  </ul>
  <a href="/births" class="btn btn-danger mt-4 w-100 w-md-auto">Back ←</a>
</body>
</html>
