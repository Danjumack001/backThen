<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About_time</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
  <link rel="icon" href="/images/favicon.png" type="image/png">
  <link rel="shortcut icon" type="image/png" href="/images/favicon.png">

  <style>
    body {
      background-color: rgb(24, 24, 24);
    }
    a, h5, div {
      font-family: monospace;
    }
  </style>
</head>
<body>

  <ul class="nav justify-content-end">
    <li class="nav-item">
      <a class="nav-link active text-danger" aria-current="page" href="#">Home</a>
    </li>
    <li class="nav-item">
      <a class="nav-link text-danger" href="about">About</a>
    </li>
  </ul>

  <div class="container text-center mt-3">
    <h4 class="text-light">Welcome , <%= user.fullname || user.name %></h4>
    <p class="text-secondary">Today's date is : <%= today %></p>
  </div>

  <div class="container d-flex justify-content-center mt-3">
    <form class="d-flex" role="search" style="width: 50%;" method="get" action="/search">
      <input class="form-control me-2" type="search" name="q" placeholder="Search something <%= user.name%>..." aria-label="Search" required>
      <button class="btn btn-danger" type="submit">Search</button>
    </form>
  </div>

  <a class="text-danger" data-bs-toggle="offcanvas" href="#offcanvas_div" role="button" aria-controls="offcanvasExample" 
     style="text-decoration: none;font-size: 50px;">
    ≡
  </a>

  <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvas_div" aria-labelledby="offcanvasExampleLabel">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title" id="offcanvasExampleLabel">About_time</h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body">
      <div>Explore  ...</div>
      <div class="dropdown mt-3">
        <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
          Click to Select ..
        </button>
        <ul class="dropdown-menu">
          <li><a class="dropdown-item" href="#"    data-bs-toggle="modal" 
   data-bs-target="#customModal">Today Search ...</a></li>
          <li><a class="dropdown-item" href="account">Account</a></li>
          <li><a class="dropdown-item" href="contact">Contact</a></li>
        </ul>
      </div>
    </div>
    <a href="/logout" class="text-danger text-center" style="text-decoration: none;">Sign-Out</a>
  </div>

  <div class="card mb-3 mt-3 bg-dark border-danger shadow " >
    <div class="card-body">
      <h5 class="card-title text-danger">Today in History</h5>
      <div class="list-group">
        <% if (clip && clip.length > 0) { %>
          <% clip.forEach(item => { %>
            <div class="list-group-item bg-dark text-white border-0">
              <h6 class="mb-1"><%= item.year %></h6>
              <p class="mb-1"><%= item.text %></p>
            </div>
          <% }) %>
        <% } else { %>
          <p class="text-muted">No detected events on this day ....</p>
        <% } %>
      </div>
      <a href="today" class="btn btn-danger btn-sm mt-2">See More</a>
    </div>
  </div>

  <div class="container mt-5">
    <div class="row">
      <div class="row-md-6">

        <div class="card bg-dark border-danger shadow h-50">
          <div class="card-body">
            <h5 class="card-title text-danger">Births on this day</h5>
            <div class="list-group">
              <% if (birth && birth.length > 0) { %>
                <% birth.forEach(item => { %>
                  <div class="list-group-item bg-dark text-white border-0">
                    <%= item.year %> — <%= item.text %>
                  </div>
                <% }) %>
              <% } else { %>
                <p class="text-muted">No births today.</p>
              <% } %>
            </div>
          </div>
          <a href="births" class="btn btn-danger btn-sm">See More</a>
        </div>

        <div class="card bg-dark border-danger shadow mt-4">
          <div class="card-body">
            <h5 class="card-title text-danger">Deaths on this day</h5>
            <div class="list-group">
              <% if (death && death.length > 0) { %>
                <% death.forEach(item => { %>
                  <div class="list-group-item bg-dark text-white border-0">
                    <%= item.year %> — <%= item.text %>
                  </div>
                <% }) %>
              <% } else { %>
                <p class="text-muted">No deaths today.</p>
              <% } %>
            </div>
            <a href="deaths" class="btn btn-danger btn-sm">See More</a>
          </div>
        </div>

      </div>
    </div>
  </div>




  <div class="modal fade" id="customModal" tabindex="-1" aria-labelledby="customModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      
      <div class="modal-header">
        <h5 class="modal-title text-danger" id="customModalLabel">Search</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      
      <div class="modal-body">
        <p>Confirm to Change Username.</p>
        <form id="searchForm" class="d-flex" role="search" style="width: 50%;" method="get" action="/today_search">
      <input class="form-control me-2" type="search" name="q" placeholder="Search todays history..." aria-label="Search" required>
      <button class="btn btn-danger" type="submit">Search</button>
    </form>
      </div>
      
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
      </div>
      
    </div>
  </div>
</div>

</body>
</html>
